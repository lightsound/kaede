{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Specs evaluate snippets inside the browser page, so they reference the
    // Window global that @maple/shared's e2eHook.ts augments with the hook.
    "lib": ["ES2022", "DOM"],
    // The config reads process.env; specs otherwise run in Node via Playwright.
    "types": ["node"]
  },
  "include": ["test", "playwright.config.ts"]
}
