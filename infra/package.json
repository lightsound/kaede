{
  "name": "@kaede/infra",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "alchemy": "cross-env npm_execpath= ALCHEMY_PROFILE=kaede NODE_OPTIONS=--experimental-strip-types alchemy",
    "deploy:prod": "cross-env npm_execpath= ALCHEMY_PROFILE=kaede NODE_OPTIONS=--experimental-strip-types alchemy deploy --stage prod",
    "plan:prod": "cross-env npm_execpath= ALCHEMY_PROFILE=kaede NODE_OPTIONS=--experimental-strip-types alchemy plan --stage prod",
    "destroy:prod": "cross-env npm_execpath= ALCHEMY_PROFILE=kaede NODE_OPTIONS=--experimental-strip-types alchemy destroy --stage prod"
  },
  "devDependencies": {
    "@effect/platform-node": "4.0.0-beta.103",
    "@types/node": "^22.20.1",
    "alchemy": "2.0.0-beta.70",
    "cross-env": "^10.1.0",
    "effect": "4.0.0-beta.103",
    "typescript": "~7.0.2",
    "wrangler": "4.119.0"
  }
}
