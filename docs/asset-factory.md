# アセットファクトリー運転手順（Phase 5 ①b）

状態: **①b 本体で整備・量産通し運転済み（2026-08-09）**（前提 ⑴〜⑷
完了済み）。規格の憲法は [asset-pipeline.md](./asset-pipeline.md)、歩行レシピは
[avatar-rig.md](./avatar-rig.md) と ROADMAP ①b(c)/(a)⑵。

## 目標状態

**発注書（`order.json`）を書けば、コードを触らずにアセットが増える。**
人のゲートは検品ビューア（`/assets`）での目視 1 回と、シート画像が見える PR
のオーナー承認だけ。

## レーンは 4 本（歩行系は発注書の `template` で自動選択）

| レーン | 用途 | 生成 | コスト実測 |
| --- | --- | --- | --- |
| **動画レーン**（`avatar-stand`） | 新キャラの stand | nano-banana-2 立ちコマ（**歩行の wan-2.7-i2v プロンプト演技は 2026-08-12 の歩行統一で全廃** — walk セルは walk レーンの担当） | 立ちコマ $0.10 |
| **シート編集レーン**（`avatar-outfit-edit`） | 服替え・素体変種 | 既存の構成済みシートへの keep-everything 編集（`editSource`）。動画不要・ポーズは構造的に保存 | $0.10/着 |
| **fal replace レーン**（`scripts/factory/replace_lane.py`） | ダンス等の動画ネイティブ頭系統をキャラ横展開 | マスターテイク（台帳 `master_takes.json`）× キャラ画像 1 枚 → wan-2.2 animate/replace（AI Gateway BYOK・480p 既定） | $0.155〜0.2425/動作（トリム済みマスター長 62〜97 コマに比例。fal 残高で照合） |
| **walk レーン**（`scripts/factory/walk_lane.py` — 2026-08-12 歩行統一・①d 論点 6） | 全シートの walk セル | 登録済み歩行マスター（プリセットモーション由来 — Meshy = 金型・新モーション鋳造時のみ）から `extract`（シートのキャラ = マスターのキャラなら直切り出し・無課金）または `replace`（identity 転写）→ cycle_scan 位相選定 → stand セル頭部の erase-then-paste 合成 | extract $0 / replace $0.155〜0.2125 |

### fal replace レーン（マスターテイク方式）

**発注書だけでキャラ×動作が増える**: 振り付け×シルエット系統ごとの承認済み
マスターテイク 1 本（`register` が緑背景・承認出所・ループ閉包を機械確認して
トリム版を R2 登録）に対し、新キャラは identity 画像 1 枚の発注書で
`produce` を呼ぶだけ。動き・タイミング・シルエット枠はマスターから完全
継承される（枠が合わないマスターは髪ボリュームが潰れる — PR #101 実測）。

```sh
export CLOUDFLARE_API_TOKEN=...
python3 scripts/factory/replace_lane.py produce order.json --budget 0.5
python3 scripts/factory/replace_lane.py costs   # fal 残高照合（BYOK はログ cost=0）
```

発注書は `{id, motion, family, identity, standSource, resolution?, useTurbo?,
cells?}`（identity / standSource は R2 sha256 かローカルパス）。produce は
台帳照合 → マスター取得（sha256 照合）→ identity 正方形化 → replace 投入
（state 永続化・--budget 停止・キー再利用ガード）→ 緑服セーフキー →
ループ検証 → **瞬き回避セル選定**（位相等価コマから眼開度最良を選ぶ）→
透過背景シート＋neckAnchors → 検品材料（マスター並列モンタージュ・96px
ループ動画・顔ストリップ）まで一気通貫。レシピは wan replace 480p・
steps 既定 20（ノブ非公開 — オーナー裁定）・品質を上げたいテイクのみ
`useTurbo`（約 3 倍遅・追加費用なし）。

### walk レーン（歩行統一 — 2026-08-12・①d 論点 6 の裁定）

**全シートの walk セルは登録済み歩行マスター 1 本から出る**（boy 系 =
`walk/boy`・girl 系 = `walk/girl`・carry 系 = `walk-carry/boy`。マスター自体は
Meshy プリセットモーションの 3D 緑参照 × seedance 清書 — 金型は新モーション
鋳造時に 1 回だけ）。旧方式（wan-2.7-i2v にプロンプトで歩行を演技させる）は
本番から全廃。

```sh
python3 scripts/factory/walk_lane.py produce <order.json> --master walk/boy --mode extract
python3 scripts/factory/run_avatar.py <order.json> --from-stage import   # 標準取り込み＋全ゲート
```

- `extract`: シートのキャラ = マスターのキャラなら**モデル呼び出しゼロ**で
  マスターからコマを直切り出し（danceMaster 前例・運転知見 19）。
- `replace`: 他 identity への転写（fal wan-2.2 animate/replace —
  シルエット系統の合うマスターを選ぶ・運転知見 18）。
- 選定は cycle_scan（台帳の機械既知周期・頭スキップなし・**台帳
  loop.start より前の ease-in 区間は走査しない** — girl の逆位相ボブは
  ループ外セル 3/9/15/21 が源流だった、差し戻し② 2026-08-13）＋ walk
  ゲート一式（頭部整合・ドリフト・leg phase＝IoU 一本・**首接合部**・
  **ボブ位相**）。アンカーは足スプレッド最大＝近側接地（3/4 視点で唯一
  信頼できる脚位相観測量 — 符号付き足シグナルはレンダーごとに反転し、
  遠側接地は奥行き相殺で通過並みに細く写る）＋丸め四分位
  （foot_phase.stride_quad）。合成は component erase ＋ alpha_composite
  （girl 首切れ 3 ラウンドの根治規則）で、頭はシートの committed stand
  セルから貼り直す — 動画側の髪は最終シートに残らない。
- **首ボブは処方制**（差し戻し② 2026-08-13 — 増幅制（ゲイン 4）を全廃）:
  合成後の首高さを「接地セル = stand −1.5%・通過セル = stand +1.5%」に
  **処方**する（compose_sheet の BOB_AMPLITUDE_FRAC ≈ p-p 3px @96px =
  オーナー承認済みの旧 boy パターン。4x=192px 出荷では p-p ≈6px）。
  マスター実測ボブの増幅は、チビ寸法では実測偏差（±1px 前後）がノイズ
  支配のため**シートごとにランダムな位相のボブ**を作る — girl の逆位相・
  carry の 7px バウンスの正体。**水平の頭ずらしは引き続き封印**（チビ頭が
  セル bbox 両端を規定 → 中心揃え＋下辺中央アンカーで全体平行移動に化け、
  顔は動かず足が滑る — 実測済み）。処方ボブ以前の committed walk シート
  （v2 手順 1 の 4x 再スケール分）はボブ位相ゲートに落ちる — 免除でなく
  v2 レーンでの再鋳造が正（test_factory の既知負債コメント参照）。
- **マスターの視点は正準右 3/4 で鋳造する**（差し戻し対応 2026-08-12）:
  replace/extract はマスターのシルエットを継承するため、緑参照が profile
  だと最終セルまで真横になる（v1 carry の棄却理由 — 後傾・脚細・シャツ
  欠けの主因）。Meshy GLB は `spike_tripo_render.py` の yaw45 で自前
  レンダーしてから seedance 清書。
- **carry は持ち物の重量で 2 系統**（オーナー裁定 2026-08-12。**heavy =
  両手持ちは存続** — 全廃しない・重量物の片手持ちは違和感、オーナー裁定
  2026-08-14）: `walk-carry/boy`（両手持ち・重い物）と
  `walk-carry-light/boy`（片手・軽い物）。item manifest の
  `carryStyle`（light/heavy）でクライアント・試着ステージがシートを
  選ぶ（light 未生産の服は heavy へフォールバック）。モーション本体の
  再鋳造は factory v2 レーン（手順 2〜4）で行う — PR #110（差し戻し
  ③〜⑤）は 4x 移行と競合するためクローズし、機械・規則のみ本節と
  ゲート 10/11・運転知見 22 へサルベージ（2026-08-14）。#110 の実測で
  確立した規則: 両手前保持の直立自然歩行プリセットはカタログ 680 本中
  **Texting_Walk_inplace がほぼ唯一**（Carry_Heavy_Object 系は空手だと
  「夢遊病」でオーナー棄却）。carry の handAnchors は**シートごと・
  フレーム別に描かれた手の上を実測**（皮膚近接 lint が検証。計測バンドが
  顎の肌・スイング側の手を掴む誤検出に注意 — 実測 2026-08-13）、
  hand.png は order の `handLayerFrom`（既定 stand）でポーズを指名して
  切り出し、`handLayerSide: near` で**手前側の手だけ**を残す — 両手
  ブロブ全体を前面に貼ると持ち物が肌色の帯で分断される（差し戻し③）。
  **「手前」は胴の前を横切る側 = 画像左**（右向き 3/4 の解剖: 奥の腕は
  体を回り込んで画像右に出る。最初の実装は画像右を残し「奥の腕が
  ぬいぐるみの前」とオーナー棄却 — 差し戻し④。正しい重なり: 手前の手→
  アイテム→奥の腕）。長袖服は手前手の肌がアンカーから分断されるため
  `handLayerSeed` で手前手に直接シードする。stand の腰ミトンを別ポーズの
  手の上に貼ると第二の手が浮いて見える（差し戻し②）。**保持腕は生成系が
  自然歩行へ引き戻す**: 参照で静止していても「stays PERFECTLY STILL」を
  明示しないと軽いスイングに化ける（v1 wan 実証文言の再確認 2026-08-13）。
  **片手静止掌の鋳造はガチャが強く未確立**（腰前掌 3 テイクとも胸で拳化/
  様式ドリフト/掌ふらつき — 2026-08-13）: light は heavy シートから
  **奥の手をピクセル消去する決定論導出**（`derive_light_carry.py` — $0・
  行バンド制限で裸体の全身肌フラッドも安全）が 3 経路比較で勝ち。ただし
  **長袖の奥腕は袖ごと伸びるため肌消去では消えない**（red で実測）—
  長袖 light 変種は boy light からの nano 服替え編集（服替えは安定・
  ポーズ変えは不安定という nano の分業）。
- 服替え変種（red/pants ± carry ± carry-light）は素体の新 walk シートへの
  **シート編集**が実測で優位（$0.10/着・IoU ゲート・変種間で振り付けが
  バイト一致に揃う）。replace の identity 差し替え（$0.155〜/着）は系統の
  合うマスターが無い新シルエット向け。フードなど**首ピンチ検出を壊す服は
  `lint.neckFrom`** でペア素体 manifest から首を転写検証する（構築不変量
  — 編集レーンの IoU ゲートが構築保持を証明）。旧 carry の staticize
  （上半身の内部色移植）は v2 マスターで腕が自然に動くため**撤去**
  （order の `staticUpperBody` で opt-in のみ — 顔 RGB の移植が二重顔の
  原因だった）。

### 3D 正本台帳（`scripts/factory/model_ledger.py` — ファクトリー v2 手順 2）

第 1 層（3D 正本 — factory-v2-plan §2）の台帳 `master_models.json`。
キャラごとのリグ済み GLB（同一性・ポーズの権威）と、カタログプリセットを
そのリグへリターゲットしたモーション GLB を、master_takes と同じ
「登録条件の機械強制」で管理する:

```sh
python3 scripts/factory/model_ledger.py register-model --family <f> \
    --rigged <sha256> --remesh <sha256> --provenance ... --approval ...
python3 scripts/factory/model_ledger.py register-motion --family <f> \
    --motion <m> --glb <sha256|path> --preset ... --approval ...
python3 scripts/factory/model_ledger.py retarget --family <f> \
    --preset <カタログ名> --budget 20   # 再リグ 5cr＋リターゲット 3cr
```

- 登録条件: R2 保管（sha256 照合）・承認出所・**骨署名ゲート**
  （`bone_signature.py` — 相対閉包 ≤ 0.35・2 周入りクリップはループ整合
  ≤ 1.0・歩行系は L−R 足先行の交代 2 回/両振幅/半周期反相の 2 歩確認 =
  運転知見 22 の機械化）・緑参照のシルエットゲート（loop_scan の
  マスター水準）。較正の実測値は bone_signature.py の docstring と
  factory-yield 2026-08-14 の行。
- 緑参照（第 2 層の清書入力）は bpy yaw45（spike_tripo_render の出力契約）
  ＋**サイクル正確なサブフレーム標本化**（`bpy_render_loop.py`）で
  タイル×3 — Meshy クリップは自前レートで作られており、24fps の整数
  コマ標本化は最大 ~0.8 コマのシームを参照に焼き込む（運転知見 18 の
  シーム毒の源）。
- モーション追加はまず登録済み GLB の再利用（walk/run はリグ付属 0cr）、
  次に retarget（運転知見 23 の恒久経路）。姿勢の質はゲート外 —
  オーナー目視の領分（girl Texting の前傾 +20.7° 実測 — 台帳 note 参照）。


## 工程

| # | 工程 | 実装 |
| --- | --- | --- |
| 1 | 発注書 | `game.package/<dir>/order.json`。`template` + `vars` で種別テンプレを展開（直書き `prompt` も可）。展開結果は `prompt` に記録（再現性） |
| 2a | 立ちコマ生成 | `google/nano-banana-2` ＋ 基準リファレンス（`canonical/`） |
| 2b | walk セル | walk レーン（`walk_lane.py` — 登録済み歩行マスターから extract / replace。旧 wan-2.7-i2v プロンプト演技は 2026-08-12 全廃） |
| 2c | コマ位相選定 | `scripts/factory/cycle_scan.py` — 足位置重心の自己相関（基本周期選好、`foot_phase.py`）で全ストライドを候補化し、**頭部・ドリフト検査に落ちたコマは位相等価コマ（±k×周期）で置換**して最初のクリーン周期を選ぶ。`--contact` で起点固定（視覚ゲートの裁量） |
| 2d | 頭部合成 | stand の頭部を各 walk コマの構造 neck へ **erase-then-paste**（動画側頭部を消してから貼る — PR #94 の二重頭の再発防止）。neck の接地相対位置が stand とずれたコマは候補フォールバック → 尽きたら fail loud |
| 2e | シート組立 | 5 コマ横一列のグリーンバックシート → `sheet-original.png` |
| 3 | 取り込み | `scripts/import-avatar-sheet.py`（R2 原本解決込み） |
| 4 | アート lint | 構造アンカー乖離・寸法・残渣・頭部整合性・コマ間パレットドリフト・（opt-in）ベース服コントラスト / ペア素体色一致。編集レーンは加えてシルエット IoU |
| 5 | アトラス化 | AssetPack（`pnpm --filter @kaede/client assets:pack`）— ビルド派生物。正は個別 PNG |
| 6 | 原本アップロード | `scripts/upload-asset-originals.py` → R2 `kaede-asset-originals` |
| 7 | PR | 取り込み結果 PNG が見える PR（run_avatar が検品モンタージュも出力）→ オーナー目視 |

運転の入口:

```sh
export CLOUDFLARE_ACCOUNT_ID=... CLOUDFLARE_API_TOKEN=...
python3 scripts/factory/run_avatar.py packages/client/src/game.package/<dir>/order.json --record-yield
python3 scripts/upload-asset-originals.py packages/client/src/game.package/<dir>/order.json
pnpm --filter @kaede/client assets:pack   # 任意・アトラス派生物の確認
```

歩留まり・コスト・所要時間は各通し運転のたびに
[factory-yield.md](./factory-yield.md) へ追記する（DP-B の判定材料）。

## 量産で守る製品ルール

- 女の子のデフォルトコーデは**パンツ一丁ではなく上のインナーを着用**
  （オーナー指定 2026-08-09）。インナーは**肌色系を選ばない** — 肌色キャミは
  ゲームスケールで裸に見える（PR #94 の棄却実測）
- 男の子のパンツ一丁ベースコーデを 1 着持ち、検品ビューア試着ステージの
  **デフォルト**にする（`studio.package/dressup.ts` の `DEFAULT_OUTFIT_ID`）
- 歩行はプリセットモーション由来のマスターテイク採用ライン（walk レーン＋
  立ちコマ頭部の neck 合成 — 2026-08-12 歩行統一）

## アート lint（①b(a)⑵ の穴への構造対応 ＋ PR #94 棄却で較正した 2 検査）

1. **neck**: 色（肌ブロブ）ではなく、シルエット幅プロファイルの
   「頭ピーク → くびれ → 胴ピーク」構造で検出。フード等で最細行ヒューリスティック
   が腰へ落ちる失敗を機械で弾く
2. **hand**: carry は腰バンド内のシルエット最大幅行の中心（ミトンパッド）。
   股下ストライドの幅ピークには乗らないようバンドを `[0.55h, 0.70h]` に制限。
   swing は発注書オーバーライド優先。shirtless の `handLayer` 切り出しは
   胴まで洪水しないよう、大きすぎる塗りつぶしを楕円キャップで打ち切る
3. **パレットコントラスト**（opt-in `lint.contrastBase`）: ベース服の主要色との
   最小距離が閾値未満なら不合格（白いアイテムが白シャツに溶ける実測への対応）
4. **頭部整合性**（新設）: 各コマの首行位置・上首部の最大幅／画素量を stand と
   比較（Δ≤3px・幅比≤1.05・画素比 0.90〜1.05 — 出荷 4 シートと PR #94 棄却品で
   較正）。二重頭（画素比 1.61）とボブ毛のはみ残り（1.07〜1.08）を機械で弾く
5. **コマ間パレットドリフト**（新設）: 各コマの**内部**有意色（境界の
   アンチエイリアス混色は除外 — 黒髪×淡色キャラで誤検出 98〜100% を実測）が
   stand パレットから距離 45 超なら不合格。wan がキャミを灰化した実ドリフト
   （57〜58）を 2 テイクで検出。同系色内の微妙な肢体トーン差（PR #94 のピンク
   肢 ≤29）は閾値では分離できず**視覚ゲートの領分**
6. **シルエット IoU**（編集レーンのみ）: 各ポーズの編集結果 vs 編集元コマの
   IoU ≥ 0.80（服替え実測 0.85〜0.97。ポーズ崩壊は contact コマが大きく落ちる）
7. **ペア素体色一致**（opt-in `lint.matchStand`）: carry 変種の stand を
   ペア素体の stand とドリフト比較。試着ステージは持ち物クリックで
   素体⇄carry を切り替えるため、肌トーンの跳びは可視欠陥（リテイクが
   白肌で返った実測から追加）
8. **首接合部**（新設 2026-08-12 — ①d 論点 6 の girl 首切れ再現）: walk 各
   コマの neck アンカー窓 ±5px・アンカー行 −4〜+2 の帯で、行ごとの実効
   アルファ幅 ≥3.0px（棄却済み girl walk-c は 2.0 = 文字どおりの頭体断絶）
   ＋最悪行の soft/solid 比 ≤1.5（半透明ブリッジ — 自己マスク paste の
   α二乗劣化も端から検出）。旧 girl セル=陽性・committed 健常セル=陰性で
   較正。stand は免除（接合部フリッカーは再生速度の歩行アーティファクト）
9. **leg phase**（2026-08-12 に IoU 一本へ再較正）: walk-a vs walk-c の
   シルエット IoU ≤ 0.90（ほぼ同一ポーズ = ストライド消失の検出）。旧
   足位置符号検査は基準系（stand 相対 vs サイクル相対）でレンダーごとに
   反転し不安定と実測 — 撤去
10. **ボブ位相**（新設 2026-08-13 — 差し戻し②の NG1/NG2 を機械化。#110
    サルベージで 4x 較正 2026-08-14）: walk 4 セルの首高さ（フレーム高 −
    首行）が「接地 walk-a/c ≤ 通過 walk-b/d −2px」の 2 山パターンで、
    振幅 p-p が [4,10]px @192px（= [2,5]px @96px 換算）。girl の逆位相
    （47/44/45/48 @96px）・carry の過剰（45/51/44/51）・却下された修正
    2 案（PR #108: 46/46/44/49・PR #109: 44/48/47/44）を全て弾く較正を
    テストに固定。処方ボブ以前の committed シートも再取り込み時に落ちる
    — 免除でなく walk レーンの再走が正
11. **手アンカーの皮膚近接**（新設 2026-08-13 — 「指先・空中アンカー」の
    機械化。#110 サルベージで 4x 較正）: carry シートの hand アンカー
    ±6px @192px に肌色画素が必須。腕先端アンカー（アイテムが顔の横に
    浮く）の再発防止。検出でなく記録値の検証（lint.neckFrom と同じ
    位置づけ）

## 運転知見（2026-08-09 通し運転）

1. **nano-banana-2 は「下着・裸」系の語で拒否風 500**（`Failed to parse
   model output`・課金ゼロ）を返しやすい。「dress-up BASE BODY sprite /
   like a swimmer / underwear shorts」の言い回しで一発通過。ジェネレータは
   5xx を 3 回まで自動リトライ（失敗は無課金）
2. **wan の倍周期**: 自己相関の argmax は 2 ストライド周期を掴むことがある
   （実測 12 コマ周期のクリップで 24 を選び、pass スロットに contact が
   落ちた）。基本周期選好（ベスト比 0.9 以内の最小ラグ）で解消
3. **wan は淡い肌・パステル服を再塗装する**（女の子で 3 テイク実測:
   キャミ灰化×2・肢体の黄/ピンク化×1）。`avatar-walk-i2v-locked`
   テンプレ＋発注書 `vars.colorNotes` で服・肌・靴の色を明示固定すると合格
4. **前方に振った腕が首の谷を塞ぐ**コマは構造 neck が誤検出する —
   compose の接地相対 neck 検査で棄却し、±1〜2 コマの近傍候補へ
   自動フォールバック（動画の取り直し不要）
5. **腕の前後交代（交差）は視覚ゲート項目**（オーナー棄却 2026-08-09 —
   「ずっと同じ手が前」）。96px のシルエットでは腕が胴に重なり機械判定
   できない（重心・突出の両信号で分離不能を実測）。プロンプト側は
   `avatar-walk-i2v-arms`（**毎歩入れ替わる**を明示・肘は脱力・モーション
   ブラー禁止）が有効 — 汎用プロンプトの「大きく振る」だけでは wan は
   同じ腕を前に置き続ける
6. **クリーン周期はクリップのどこかにあればよい**: 自動選定した 1 周期が
   ドリフトで落ちても、全ストライド走査＋位相等価置換（cycle_scan.py）が
   同じクリップ内から救済する（girl の採用テイクは走査救済 — 取り直し
   0 本）。**位相を保存しない**近傍置換は周期の等間隔を壊し逆走
   （ムーンウォーク）に見える — 置換は必ず ±k×周期を先に試す。
   置換は**落ちたスロット自身から正確に ±k×周期**で取ること: 別ストライドの
   同スロット値を人手で流用した ~1 コマの位相ずれが「walk-a ≈ walk-d・
   中間地点なし」に見えた（オーナー指摘 2026-08-09 ラウンド 2。
   cycle_scan.py の自動置換はこの規則を守るため再発しない）
7. **ミラー合成（接地＋通過を左右反転して逆相を作る）は不採用**:
   位相・色は完璧だが、3/4 ビューの照明とキャミストラップが半周期ごとに
   左右反転してストロボになる（videoReview 棄却 2026-08-09）。頭部は
   合成で無事でも体の描き込みが向きを持つ
8. **carry シートは取り込み前に静止上半身の内部色移植**
   （`staticize_carry_sheet` — 脚の開始行より上で、walk セルの不透明画素へ
   stand の RGB を転写。輪郭・アルファは各セル維持なので継ぎ目なし）。
   シート一括編集はセルごとに腹部陰影を微妙に描き分け、再生時に
   フリッカーする（オーナー棄却 2026-08-09）— 「腕・胴は静止」という
   carry 仕様を文字通りピクセルで強制する（頭部合成の規則の拡張）。
   丸ごと差し替え案はシルエット差（最大 18%/行）が継ぎ目の段差になるため
   内部色のみ
9. **脚位相ゲートは stand 相対で測る**（girl やり直しラウンド 2026-08-09）:
   足バンド重心の生信号はキャラ固有のオフセットを持つ（girl は対称な直立
   stand でも +2.2、boy は +0.4）。接地 2 コマは stand 相対の信号が
   **逆符号かつ |sig|≥1.2**（承認済み swing シート実測 a'−2.2〜−2.6 /
   c'+2.4〜+2.7）。carry シートは穏やかなストライドのため除外
10. **全ペアの複製上限**: walk 4 コマのどのペアも IoU ≤ 0.90（承認済みの
    最大は (b,d)=0.87）。「同じ脚の接地 2 回」（IoU 0.97 実測）や置換の
    取り違え（(b,c)=0.95）を機械で弾く — オーナーの「片足スキップ・
    中間がない」棄却 2 回をコード化したもの
11. **位相等価置換は隣接ストライド（±1 周期）のみ**: 離れた領域のコマは
    画素ゲートを通ってもレンダリングスタイルが違う（ピンク脚を実測）
12. **静止画モデルによる歩行コマの生成・リスキンは不採用を再々確認**:
    boy シートの全シート/コマ単位 nano リスキンはどちらも脚ポーズを
    軟化・収束させる（①a の「逆足を描き分けられない」の同型）。新キャラの
    歩行は動画レーン一択
13. **動画モデルの選択知見**（girl 実測）: wan はパステル淡色キャラで
    色再塗装が頻発（`avatar-walk-i2v-arms-soft` が脚交代の最良オッズ）。
    seedance-2.0 は色保持が強い一方、i2v は「その場行進」化しやすく、
    **色の置換**（青キャミ→クリーム — 距離ベースのドリフト検査を回避する）
    も起こす。r2v（reference_video モーション転写）は参照が Web URL 必須・
    ≥409,600 画素で、周期内の第 2 接地が痩せる実測 — どのモデルでも
    最終権威はゲート一式＋目視
14. **96px の歩行良否はレビュー AI も不安定**（IoU 0.62 の別ポーズ対を
    「同一」と判定する等、計測と矛盾した判定を複数回実測）。機械ゲート
    （較正済み）で絞り、**最終判定はオーナー目視** — レビュー AI の
    verdict を単独のゲートにしない
15. **緑服キャラとクロマキーの衝突**（fal replace レーン e2e 2026-08-11）:
    既定キー（hard60/soft20＋全画素 despill）はオタクの緑チェックシャツを
    食い、despill が残りを灰化する。replace 出力では背景緑のドミナンスが
    150+・柄の緑が 20〜60 と分離可能で、レーンは「定域 100 で抜き＋境界
    2px リングのみソフトキー/despill」の緑服セーフキーを使う。ただし
    **標準インポータの全画素 despill は透過背景シート経由でも緑服を灰化**
    （柄シャツの緑優勢画素 20.7%→4.2% 実測）— インポータ側の対処までは
    緑主体の衣装をレーンの禁則にする（実写クロマキー現場の衣装規則と同型）
16. **replace 出力のループ指標はマスターより系統的に低い**（identity
    再描画ノイズ: メガネ・柄のゆらぎで数ポイント低下）。ゲートは 2 系統 —
    マスター登録はループ整合 0.94/閉包 0.93（合格帯 0.972〜0.993 で較正）、
    produce 出力は 0.89/0.92（合格出力 0.910〜0.961/0.949〜0.985 と唯一の
    既知不良サイクル 0.869 を分離）。周期はマスターから継承されるため
    produce は周期固定で品質のみ再証明する
17. **ループ探索は (start, period) を「ループ全幅の整合」で同時最適化する**
    （`loop_scan.py`）: 単一コマ閉包はポーズの偶然一致で誤った周期に高得点
    を出す（girl gangnam 清書は誤周期 44 で閉包 0.976 だがループ全幅は
    0.87 — この形を機械で弾くのが登録ゲート）。倍周期は基本周期選好
    （foot_phase と同型）で除外
18. **3D モーション資産のループ品質はリターゲットごとのガチャ**
    （girl gangnam 実測 2026-08-12）: 同じ Meshy プリセット
    （Gangnam_Groove）でも boy リターゲットは骨署名の相対閉包 0.076 の
    クリーンループ、girl リターゲットは最良 0.983 — どの周期でも
    「1 コマ分の動き」相当の継ぎ目が残り、この継ぎ目つき参照を seedance
    が不均一に平滑化したものが旧不合格候補の「周期不均一」の正体。
    参照動画は必ず**機械確認済みクリーンループのタイル×N** から作る
    （bpy 全コマレンダー → chroma_key → `loop_scan` が確認手順）。
    リターゲットがループしないキャラでも、identity 画像でシルエットごと
    描き直す seedance 清書（crosschar 方式 — boy 参照×girl stand セルで
    girl gangnam マスター確立）ならマスターを作れる。**wan replace は
    この代役に使えない**: 動き・タイミングに加えて元動画キャラの特徴
    （3D メッシュに焼き込まれた両側ピン・服の形）まで継承する（480p
    対照実験で identity 崩壊を実測 — シルエット枠継承の裏面）
19. **登録済みマスターはそのキャラ自身のダンスセルの無課金ソース**
    （girl シート本採用 2026-08-12）: `compose_gesture_sheet.py` の
    `danceMaster` モードが台帳の (start, period) を位相アンカーに
    blink 選定＋髪ブロブ中央値正規化で 8 セルを直切り出す — replace
    呼び出し不要・選定は決定論（/tmp 検証との再導出一致を実測）。
    あわせて **nano 静止ポーズ編集のリテイクパターン 3 種**を実測:
    ①「no ground」指示でも**地面線を描き込む**ことがある（クロマキー後も
    残存 — 線の明示禁止でリテイク）②スケール指示を入れても**キャラを
    拡大描画**することがある（髪スケールゲートが捕捉 — 1.158 棄却の実測）
    ③服に**新しいシェーディング色を発明**する（キャミの灰緑 #9eb5ba
    距離 60 を較正済みドリフト lint が捕捉 — フラット色固定の文言で解消）。
    いずれも既存ゲートが機械検出するので、リテイクは文言調整のみで済む
20. **出荷スケール変更は基準器も動かす**（4x=192px 再取り込み・ファクトリー
    v2 手順 1、2026-08-14）: px 長の較正値は倍率に合わせて機械的に倍にする
    （art_lint の NECK_DIVERGENCE_PX ほか — 論理許容は不変）だけでは足りず、
    **比率ゲートの基準器そのものがずれる**。高精細化した stand は輪郭の
    にじみ画素が減って参照髪ブロブが ~3.5% 縮む（girl 実測 170.4→164.5）
    ため、全セルの髪スケール比が ~4% 上振れし、承認済み girl sleep
    （旧基準器 1.118）が同一ピクセルのまま 1.159 を計測 —
    HAIR_SCALE_TOLERANCE を 0.15→0.18 へ実測再較正した（検出対象の
    1.42x/2x 取り違えからは依然 1 桁遠い）。また `lint_avatar` は歩行
    シート較正のゲートで、**ジェスチャーシートの正式ゲートは compose 時の
    `check_gesture_cell`**（腕上げ・横臥は構造 neck が壊れる設計事実）—
    main 時点の gesture manifest も lint_avatar は不合格で、再取り込みの
    合否判定に使わない
21. **px 長の倍化原則は「探索の局所性」には適用しない**(4x 再取り込みの
    Bugbot 対応、2026-08-14): hand レイヤー抽出のシード窓(±6px)は
    アンカー近傍で skin を見つけるためだけの窓で、flood がそこから連結成分
    全体へ育つため、切り出しサイズは作画にスケールしこの値にはスケール
    しない。倍化(±12)を実測したところ、種がミトンを越えてカーキ色ズボン
    (`is_skin` を通過する肌近似色)へ漏れ、旧 96px 時代の hand.png に
    混入していたズボン筋汚染を 2 倍解像度で再付着させた(キャラ脇に浮遊
    描画される視覚退行)。旧 red-carry hand.png の 16x24 という寸法自体が
    この汚染品で、192px の純ミトン 19x17 が正しい実寸 2 倍。較正値を
    機械倍化する際は「許容量(tolerance)」と「探索窓(locality)」を
    区別すること
22. **歩行マスターのループ窓は「2 歩入り」をボーンで確認してから鋳造する**
    （carry v2 の棄却根因 2026-08-13 — #110 サルベージ 2026-08-14）:
    シルエット IoU のループ検出は 3/4 チビの左右鏡像ステップを区別できず、
    **片脚 1 歩分の半サイクル**を「閉じたループ」として合格させる
    （v2 = Carry_Heavy_Object_Walk の 42 コマ窓・閉包 0.978 — GLB ボーン
    実測で L−R 足位相が全周期同符号と確定）。シートは「開・開・閉・閉」の
    片足スキップになり、2D 側の機械検定は不能（足シグナルの符号は
    レンダー依存・遠側接地は奥行き相殺で細く写る — 両方実測）。**金型
    （GLB）段階でボーン署名から真のフルサイクルを特定してからレンダー
    する**のが唯一堅い手順（carry v3: f54–f109・T=56 を step2 で 28 コマ・
    タイル×3）。ついでに fcurve へのクォータニオン合成で腕ポーズも直せる
    （v3 は肩 X+30°/肘 X−20° で掌を胸前→腰前へ — Meshy プリセットの
    箱前提の腕は空手だと「夢遊病」に見える）
23. **Meshy のタスクは ~3 日で削除される — R2 保管 GLB＋`model_url` 再リグが
    リターゲットの恒久経路**（3D 正本台帳の設営 2026-08-14）: 2026-08-10/11 の
    全タスク（boy リグ 019febfd…・girl 一式 019fee46…系）が単体 GET でも 404 と
    実測。「キャラごとにリターゲット 3cr」の前提だった rig タスク ID は恒久
    資産ではないため、台帳へ恒久情報として記録しない。恒久なのは R2 の
    remesh GLB で、rigging API の `model_url` 入力（fal CDN ホスト経由）に
    渡す再リグ 5cr で新しい rig タスクを立ててから 3cr/本でリターゲットする
    （`model_ledger.py retarget` — 一発成功・再リグ産 walking も骨署名閉包
    0.000 でリグ再現性を確認）。**リターゲット姿勢はキャラごとのガチャ**
    （運転知見 18 のループ品質ガチャの姿勢版）: girl の Texting_Walk は前傾
    +20.7°（hips→head。boy +1.8°・girl 自身の walking +12.2°）— 姿勢の質は
    骨署名ゲートの外でオーナー目視の領分、補正は fcurve 合成（運転知見 22）
    か boy 参照の crosschar が代替
24. **30fps 素材の 24fps 整数コマ標本化はサブコマのシームを作る**（3D 正本
    台帳の設営 2026-08-14）: Meshy クリップは自前レート（rig 付属 walking の
    真のサイクルは 24fps 換算 24.8 コマ）のため、整数コマだけで測る・
    レンダーすると最大 ~0.8 コマ分の偽シームが閉包にもタイル参照にも乗る
    （girl gangnam 級のシーム毒 = 運転知見 18 の一因になり得る）。骨署名の
    解析・緑参照レンダーとも **1/4 コマのサブフレーム標本化**で行う
    （`bpy_dump_bones.py`／`bpy_render_loop.py` — `frame_set(f, subframe=…)`
    が姿勢を補間評価しレンダーにも反映されることを画素で実測確認）
25. **Blender ヘッドレスの落とし穴 3 件**（同上）: ① スクリプト例外でも
    exit 0 で終わる — `--python-exit-code 1` を必ず付ける（付けないと
    「レンダー 0 コマ」のような下流症状で発覚する）② Cycles addon は `--`
    以降の引数を自前 argparse する — `--cycle` は `--cycles-print-stats/
    --cycles-device` と曖昧衝突して **addon 登録ごと失敗し CYCLES エンジンが
    消えたように見える**（引数名を `--span` に改名して回避）③ ディストロの
    blender パッケージは Cycles が壊れていることがある — 公式 tarball
    ビルドを使う（`BLENDER_BIN` で指定可能）
26. **recast（3D 緑参照 × 原本 identity の replace）はコマ 1:1 同期を
    保証し、3D 由来メタデータをそのまま清書コマへ持ち込める**（第 3 層
    スパイク 2026-08-15）: replace はタイミングを継承する（出力コマ数 =
    参照コマ数 — `recast` が機械強制）ため、register の `sourceStart`
    記録だけで「マスターコマ i ↔ 参照コマ sourceStart+i ↔ ID レンダー
    位相 (sourceStart+i) mod 周期」の対応が決定論で成立する。腕 ID
    レンダー（参照と同カメラ・同ループ窓）はマスターに対しても位置
    ズレ ≲2px@960px（清書の identity 再描画が 3D マスク縁を僅かに
    超えるだけ — シルエット IoU は参照 0.97 / 清書 ~0.87）で、出荷
    スケールの **dilate 2px＋near 優先**で完全吸収できる。層分割は
    「厳密分割」（全画素がちょうど 1 層に属す）にする — body→far→near
    の合成が元コマとバイト一致になり、アイテムを far と near の間に
    差し込んでも継ぎ目リスクがゼロになる
27. **glTF のリーフボーンの tail は importer の捏造**（同上）: 手ボーン
    のような子なしボーンは glTF に tail 概念がなく、Blender importer が
    向き・長さを発明する — mid/tail の投影は使わず **head だけ**を
    投影・記録する（`bpy_render_arm_ids.py` の bones.json）。手前/奥の
    腕判定は両手ボーン head のカメラ深度比較（`world_to_camera_view`
    の z）で全コマ機械決定でき、解剖学の目視判定（旧 handLayerSide）を
    置き換える
28. **首ボブ検定は chin 埋まりで under-read する — crown 併用の 2 信号
    ゲート**（同上）: 処方 LOW 貼りが顎を肩ラインに埋めると、幅
    プロファイルのくびれ（neck 信号）が実移動より小さく出る（第 3 層
    carry マスターで実測: 400 作業スケールの処方 12px 移動に対し crown
    =コマ高は 12px・neck ピンチは 8px）。コマは接地基準でレンダーされる
    ため **crown（コマ高）はプレイヤーが見るボブそのもの** — neck か
    crown の**どちらか**がパターンを満たせば合格・両方凍結/暴走なら不合格
    の 2 信号ゲートに較正（art_lint `check_bob_phase`）
29. **第 3 層（3D 腕マスク）方式は「出荷絵の総再描画」を構造的に強制
    する — 棄却**（手順 3 オーナー判定 2026-08-15・PR #116）: マスクは
    3D 参照とコマ同期したマスター（recast）にしか画素整合しないため、
    承認済み committed セルには当てられない。つまりレイヤーが欲しい
    シートは全セル（stand 含む）のボディを清書モデルの再描画コマへ
    総取り替えするしかなく、**差し戻しラウンドで磨いた承認済みアートを
    毎回捨てる方式**になる。機械ゲート（重なり順・分割の厳密性・ボブ）
    と videoReview が PASS しても、絵の品質（プロポーション・線・
    かわいさ）はゲート外で全面劣化し得る — 運転知見 14 の「最終判定は
    オーナー目視」の盲点がこの方式では最大化する構図。レイヤー構造の
    正しさと引き換えに承認済みアートを失うトレードは不成立 — 重なり
    表現は承認済みシート＋handLayer（現状）を維持し、実運用で問題化
    したら案 2（焼き込み）を再検討する
30. **PixelLab の画風追従はツール依存で分裂する**（手順 5 ベンチ
    2026-08-15）: image-to-pixelart 変換・generate-with-style-v2（style
    参照生成 — 96px 参照で 4 サンプル/20 世代）・create-character-v3
    （参照モード — 192px 入力から 112×192 の 8 方向回転を 3 世代で一発・
    全方向で同一性保持）は kaede の柔らかいチビ絵柄を保つが、**Bitforge
    は style_image＋strength 100 でもドット絵ハウススタイル（ディザ髪・
    褐色肌・頭身低下）へ引き戻す**。**テンプレ歩行アニメは品質不合格
    水準**（walking-6-frames: SE はストライド消失・E は片脚スキップ＋
    腕振りゼロ＋輪郭の毎コマ煮え — 3D レンダー系スパイクと同型の症状。
    静止回転は有望・動きはそのまま使えない）。計測はトライアルの usage
    表示が世代/USD 混在（アニメ job は usd 表示で実際は世代 3 消費）—
    fal 同様、**残高 API（GET /balance）の前後照合が正**。style 参照の
    出力枚数は参照画像の最大辺で決まる（43-85px=16 枚・86-170px=4 枚・
    171-512px=1 枚 — どれも ~20 世代）ため、同一コストならサンプル数の
    多い小参照が判定効率で優位
31. **r2v 世代はコマ同期しない — 周期継承はモデル依存のガチャ**（手順 4a
    ベンチ 2026-08-15）: replace の「出力コマ数 = 参照コマ数」保証（運転
    知見 26）は r2v 世代にはなく、fps も勝手に変わる（wan 2.7 系は 30fps・
    SCAIL-2 は 16fps）。周期はモデルごとに分裂 — seedance 2.5 r2v は参照
    リズムを継承（1.00s vs 参照 1.042s）、Kling o3 pro は 1.46s に間延び
    ＋足滑り、**wan 2.7 r2v は歩行モーション自体を転写せずほぼ静止**
    （reference_video_urls は同一被写体の外観+動き参照の設計で、identity
    画像と別キャラの動き転写には効かないと読める — 1 テイク即打ち切り）。
    採用時は周期を出力から再計測する（`spike_r2v_bench.py` の
    measure_loop）。**課金も llms.txt の名目より高い実測**: wan 2.7 r2v は
    明記どおり入力+出力秒（$0.76/テイク）、H3 は明記がないが入力動画秒が
    乗る（$0.57 実測 vs 名目 5s×$0.08=$0.40）、seedance 2.5 は $1.09/
    テイク（480p 4s+入力 2s —     トークン式の名目 $0.57 の約 2 倍）。
    edit-video は walk 実測で出力秒のみ（$0.21）だったが、carry 側の
    残高差分は ~$0.60 と一致せず — 差分帰属は近似で、遅延訂正課金の
    疑いを記録。**fal 残高 API の前後照合が正**
    （運転知見 30 の fal 版）で、宣言上限より実残高が小さい場合は残高が
    実効上限 — ベンチツールは送信前の残高フロア停止を持つこと。運用注意
    2 件: ①同一 workdir の FalJobs 並行実行は state.json を相互上書きし
    支払済みレコードを消す（実測 — 復旧は同一キー再実行の応答再取得。
    運転は直列が正）②wan 2.7 edit-video はキューが深い（qpos 60+・
    壁時計 ~1 時間/本 — 30 分ポーリング上限は再実行で再開）
32. **identity の 2 大運転規則 — 服装をプロンプトに書かない・入力は R2
    原本セル**（手順 4a のベンチ運転ミス 2 件をオーナー指摘で訂正
    2026-08-15）: ①ベンチのプロンプトが元画像の服装を実物と違う内容で
    誤記述（「白 T・短パン」— 実物は白 Y シャツ+紺長ズボン）し、
    **テキスト追従モデル（seedance 2.5 実測）は誤記述どおりに identity
    画像を上書きした** — 同一性の判定が丸ごと反転する汚染。プロンプトに
    服装・外見を書かず、**identity の権威は画像だけに置く**のが正
    （どうしても書くなら実物の画素確認後）。テキストが画像を上書き
    できる事実は着せ替えの制御レバーとしては別途有望。②identity 入力に
    committed の**出荷用 192px セルを使ってはいけない** — R2 に原本
    （sheet-original・stand セル実寸 ~380×418）が保管されており、
    そちらが identity の正（手順 3 recast の前例どおり。§4 解像度方針
    「静止画は高解像度」とも整合）。低解像度 identity は全レーンの
    描き直し精度を一様に下げ、ベンチの絶対品質も過小評価になる
33. **r2v の identity はポーズ/視点まで出力を上書きする — 高解像度化は
    「正準右 3/4 のまま」行うこと**（identity 解像度 A/B・オーナー承認
    2026-08-15）: seedance 2.5 720p の同条件 A/B で、identity を原本
    stand セル（右 3/4・400px）→ 高解像度 A ポーズ（正面・784px）に
    替えると**顔の可愛さ・服のディテールは明確に向上する一方、出力の
    歩行が正面向きに化けて正準右 3/4 が壊れる**（videoReview「completely
    overrides the motion reference's spatial orientation」— 緑参照の
    視点よりも identity のポーズが勝つ）。wan replace（v1）は視点を元
    動画から厳密継承するためこの結合がなく、A ポーズ identity の従来
    運用は replace 限定の作法だった。r2v 世代でディテールと視点を両取り
    するには「右 3/4 の高解像度 identity」が必要 — **レバー ①（stand
    原本の SeedVR2 画像 4x アップスケール・$0.003・ポーズ不変）を同日
    検証し勝ち確定**: 視点は右 3/4 を完全保持したまま顔・襟/ボタン/
    ベルト・輪郭安定が「meaningfully better」（videoReview 全項目で
    優位・ループ 0.989/0.991 は全テイク中最良 — マスター候補）。
    **r2v レーンの identity は「R2 原本 → SeedVR2 4x → 白地正方形化」を
    標準前処理とする**。**追補（同日 — オーナー実見「A ポーズ版の品質が
    圧倒的に上」を受けた正面化の修正 2 経路の実測）**: 高解像度 A ポーズ
    identity の品質優位は解像度でなく**原画そのものの情報量**（大判の
    描き込み）に由来し、①視点ロック文言（「STRICT right-facing 3/4
    side view・NEVER face the camera」— 外見記述でないため知見 32 に
    抵触しない）だけで正面化はほぼ解消（残差: 正解よりやや正面寄りの
    角度・videoReview は安定性最良と評価）②A ポーズ（見た目）＋立ちセル
    （角度）の 2 枚条件付けは角度が正解と完全一致する一方、奥脚の変形
    アーティファクトが出る。**大判原画を identity に使えるなら SeedVR2
    4x 前処理より上** — 新キャラは 2K 静止画方針でこの経路が既定になる
34. **判定材料の必須記載（オーナー指示 2026-08-15）**: オーナーに出す
    判定シート・比較動画は、**各行/各列にラベルを画像へ焼き込む**
    （番号・モデル・解像度・入力・実測料金）。第 1 行は必ず元画像
    （正解）。「どれがどれか」をチャット本文の説明に依存させない —
    日本語フォントは `wqy-microhei.ttc`（DroidSansFallback はラテン
    文字が豆腐化する実測）
35. **wan-animate-2（fal hosted）の運転規則**（手順 4b ベンチ 2026-08-18）:
    ①**背景とフレーミングの権威は identity 画像** — プロンプトは「外見と
    背景を記述する」仕様だが、蒸留版既定（guidance_scale 1 = CFG 無効）
    ではほぼ効かない（緑背景指定を無視して identity の白地を背景に採用
    した実測）。緑背景が要るなら **identity 自体を緑地化**する（境界
    フラッドフィルでキャラ画素は不変）②**全高 identity は出力フレーミング
    まで継承して二重に事故る** — 頭髪・足がフレーム端に接して切れ、
    駆動被写体（3D レンダー）と枠が合わず**消し残りの白ゴースト**が出る
    （既定 steps10 で 3/3 テイク）。**identity は駆動動画の被写体枠に
    一致させて配置**（walk 参照は高 43%/上 7% — `matched_identity`）する
    のが処方。ゴースト残渣は **num_inference_steps 20 でほぼ解消**
    （コスト ×1.7 — 蒸留既定 10 の消し込み不足）③**コマ同期は 1:1**
    （出力コマ数・fps = 駆動動画 — r2v 世代と違い replace と同じ保証。
    recast の決定論対応 = 運転知見 26 が成立）④**課金は compute-second
    実測 480p 2 周期 $0.052・720p $0.14・steps 比例**で、**完了の ~2 分後
    に遅延着金** — 残高照合は着金を待って行う（`spike_r2v_bench.py
    settle`）。SCAIL-2 は名目 $0.20/秒に対し実測 $0.106/秒と逆に半額
    （名目より高い運転知見 31 の逆例 — どちら向きにも残高照合が正）
    ⑤**手の向き・ゴーストの出方はテイクごとのガチャ**（720p carry 実測:
    t1 = 奥の手が上向き（オーナー指摘）・t2 = 背中ゴースト・t3 = 頭
    ゴースト・**steps30 の t4 で両方解消** — 480p でもコマによって手の
    向きが反転する、オーナー実見 2026-08-19）。リテイクの検品は数コマの
    抜き見でなく**全コマの手元ズーム帯**で行う（`hands_all` ストリップ）
