# maple-like

メイプルストーリー風2D横スクロールMMORPGの最小プロトタイプ（MVP）です。
複数のブラウザウィンドウから同じワールドに接続し、互いのキャラクターがリアルタイムに同期して動く様子を確認できます。

## 技術スタック

- **TypeScript** — 全パッケージ共通の言語
- **PixiJS v8** — 2Dレンダリング
- **Vite + React** — クライアントのビルド／開発サーバー（Reactの役割はキャンバスのマウントのみ）
- **SpacetimeDB 2.x** — リアルタイム同期バックエンド（DB兼サーバーロジック）
- **pnpm workspaces** — モノレポ管理

## パッケージ構成

| パッケージ | 説明 |
| --- | --- |
| `packages/shared` | 物理シミュレーション・マップ・定数など、クライアントとサーバーで共有するロジック |
| `packages/client` | PixiJS + React のクライアント（ローカル操作の描画とネットワーク同期） |
| `packages/server` | SpacetimeDB モジュール（`player` / `mob` / `message` テーブルと `submit_inputs` / `join` / `set_name` / `send_message` リデューサー、モブAIを駆動するスケジュールリデューサー。サーバー権威で物理・戦闘を再生し、行を切断後も保持） |

## 同期方式

サーバー権威（server-authoritative）モデルです。

- **クライアントは入力のみを送信** します。位置や速度は一切送りません。クライアントは
  `submit_inputs` リデューサーへ、`packInput` で u8 のビットフラグにパックした入力バッチを
  送ります（`INPUT_FLUSH_INTERVAL_MS` 間隔、`INPUT_BATCH_MAX_TICKS` ティックごとに分割）。
- **サーバーが共有物理で再生** します。サーバーは `packages/shared` の決定論的な
  `stepPlayer` を使い、受け取った入力を権威状態（`player` 行）へ適用します。`row.tick` が
  適用済みティック数となり、行の更新そのものがクライアントへの ack になります。
- **クライアント予測 + reconciliation（リプレイ）**。クライアントはローカルで同じ物理を
  即座に実行して予測描画し、各ティックの予測状態を保持します。サーバーから ack 行が届くと、
  対応するティックの予測状態と権威状態を厳密に比較します。
  - 一致すれば、そのティックまでの履歴を破棄して確定（reconciliation 不要）。
  - 不一致（または予測が残っていない）なら、権威状態から未 ack の入力を `stepPlayer` で
    再生して現在ティックまで巻き戻し→再構築し、`resetLocal` で描画をスナップします。
  決定論的物理のため、正直なプレイでは予測と権威は毎回ビット単位で一致し、reconciliation は
  発生しません。
- **ガード**。サーバーは速度ハック等を防ぐため、ティック数がスポーン以降の実時間ティック数を
  `TICK_ALLOWANCE_SLACK` 以上に超えないこと、バッチサイズが `INPUT_BATCH_MAX_TICKS` 以内で
  あること、`startTick === row.tick`（順序どおり・重複でないこと）を検証し、満たさない
  バッチは無視します。クライアント側には `RESEND_TIMEOUT_MS` の再送ウォッチドッグがあり、
  ack が途絶えると未 ack の入力を再送します（`startTick` チェックにより重複送信は無害）。
- **永続化とプレゼンス**。`player` 行は切断しても削除されず、`online` フラグが `false` に
  なるだけです。再接続すると同じ identity の行を再利用し、**位置と名前が残ったまま**世界に
  復帰します（今後の XP・レベル・アイテム等の進行もこの行に積まれます）。再接続時には
  `tick` を 0 に、`simStartAt` を現在時刻にリセットして速度ハックガードを張り直します
  （これをしないと、元のスポーンからの経過実時間ぶんだけ一気にティックを進められてしまうため）。
  identity は認証トークンとして `localStorage`（キー `maple.token`）に保存し、リロード後も
  同じプレイヤーとして再開します。クライアントは `online === true` の行だけを描画し、相手が
  オフラインになった行はビューから取り除きます（自分のオフライン行からは予測を開始しません）。
- **名前**。起動時のオーバーレイで入力した名前は `set_name` リデューサーでサーバーに反映され、
  `localStorage`（キー `maple.name`）にも保存して次回以降は自動適用します。サーバー側で
  トリム・最大16文字・制御文字の除去を検証します。

## モンスターと戦闘

- **サーバー駆動のモブ**。モンスター（`mob` テーブル）はクライアントが予測しません。サーバー側の
  **10Hz のスケジュールリデューサー**（`mob_ai_timer` → `mobTick`）が巡回・再出現・接触ダメージを
  すべて処理し、`mob` 行を更新します。クライアントはこの公開テーブルを購読し、リモートプレイヤーと
  同じ補間機構で滑らかに描画します（`vx = dir × speed` を速度として補間）。`mob_ai_timer` は
  非公開（private）なので、クライアントからは見えず呼び出せません。
- **攻撃はサーバー権威**。攻撃は入力ビット（`Z` キー／タッチの攻撃ボタン）として送られ、移動と
  まったく同じ経路でサーバーが共有物理の `stepPlayer` で再生します。スイングが当たったかどうかは
  サーバーが当時の `mob` テーブルに対して判定するため、**クライアントは命中を偽装できません**。
  攻撃にはクールダウン（`ATTACK_COOLDOWN_TICKS`）があり、ロープにつかまっている間は出せません。
  自分のスイングだけはクライアント予測でスラッシュ表示します（命中判定は行いません）。
- **接触ダメージと無敵時間**。アライブなモブに重なると `touchDamage` を受け、`PLAYER_INVULN_MS`
  の間は無敵になります。HP が 0 になると**スポーン地点に戻り全回復**します（テレポートは既存の
  reconciliation 経路でクライアントに反映されます）。
- **再出現**。倒したモブは `MOB_RESPAWN_MS`（5秒）後にスポーン位置へ復活します。
- **XP・レベル**。モブを倒すと経験値が入り、`xpToNext(level)` を超えると**レベルアップ**します
  （余剰 XP は次のレベルに繰り越し）。レベルアップで**最大 HP が増加し、HP は全回復**します。
  画面左下の HUD に HP（赤）と次のレベルまでの XP（黄）、`Lv.N` を表示します。

## チャット

- **ワールドチャット**。`Enter` でチャット入力欄にフォーカスし、メッセージを送ると全員に届きます。
  `Esc` でゲーム操作に戻ります（入力欄にフォーカスがある間はキー入力がゲームへ流れないので、
  チャットに「z」と打っても剣を振りません）。
- **吹き出しとログの両方に表示**。届いたメッセージは送信者の頭上に約4秒の吹き出しとして出るほか、
  画面左下のログ（直近6件、新しいものが下）にも積まれます。
- **サーバー側検証とレート制限**。`message` テーブルは公開で、クライアントは `send_message`
  リデューサー経由でのみ書き込めます。サーバーはトリム・最大120文字・制御文字の除去を検証し
  （名前と同じ検証ロジックを共有）、同一プレイヤーからの連投を **500ms** のレート制限で弾きます。
  テーブルは**最新50件だけを保持**するよう古い行から枝刈りされ、ストレージとレート制限のスキャンを
  両方とも一定コストに抑えます。`name` は送信時点の表示名のスナップショットなので、後から改名しても
  履歴は書き換わりません。

## マップとポータル

- **2マップ構成**。ワールドは2つのマップで構成されます。`MAPS[0]`「はじまりの草原」
  （スタート地点・町）と `MAPS[1]`「くらやみの森」（より密集した足場とロープ、強敵 golem）。
  両マップは**まったく同じ寸法（3840x720）**なので、カメラのクランプと視差背景は一切変更不要で、
  切り替え時に差し替えるのは**前景の地形コンテナだけ**です。
- **ポータルで移動**。各マップにはポータルがあり、**足元のポータルに重なって `↑`** を押すと
  対岸のマップへワープします。草原の右端のポータル → 森の左側、森の左端のポータル → 草原の右側。
- **移動も入力リプレイ内で決定論的に処理**。ポータル移動は専用のリデューサーを増やさず、
  共有物理 `stepPlayer` の**先頭でポータル判定**を行います（接地中＋範囲内＋`↑`）。`mapId` は
  `PlayerState`／`player` 行の一部なので、クライアント予測とサーバー再生が同じコードで
  **同時にマップを切り替え**ます。マップ移動も他の操作と同じ入力リプレイに乗るため、**特別な
  reconciliation は不要**で、**サーバー権威がそのまま保たれます**（クライアントは行き先を偽装
  できません）。
- **ピンポン防止**。到着時の座標は到着先ポータルの判定範囲の**外側（約80pxずらし）**に置いてあり、
  さらに到着直後は `onGround=false` なので、`↑` を押しっぱなしにしても**2マップ間を往復しません**。
  この不変条件は shared のテストで全ポータルについて検証しています。
- **新モブ golem**。森には強敵 **golem**（高HP・高接触ダメージ・低速）が地上を巡回します。
- **死亡時は最初のマップに戻る**。HP が 0 になると**最初のマップ（草原）のスポーン地点に全回復で
  復帰**します（テレポートとマップ切り替えは既存の reconciliation 経路で反映されます）。
- **マップごとの可視範囲**。別マップにいるプレイヤー・モブ・吹き出しは**見えません**（オフラインの
  行と同じ扱いでビューを持ちません）。**チャットログは全マップ共通**です（吹き出しは同じマップの
  相手にのみ自然に出ます）。

## 確認手順

1. **SpacetimeDB CLIのインストール**
   [https://spacetimedb.com/install](https://spacetimedb.com/install) の案内に従ってインストールします。
   （GitHub Releases の `spacetime-x86_64-unknown-linux-gnu.tar.gz` などのバイナリを直接配置しても構いません。）

2. **依存関係のインストール**

   ```sh
   pnpm install
   ```

3. **SpacetimeDB の起動**（別ターミナルで実行し、起動したままにしておきます）

   ```sh
   spacetime start
   ```

4. **サーバーモジュールの publish**（リポジトリルートで実行。`spacetime.json` が `packages/server` を指しています）

   ```sh
   spacetime publish maple-like --server local --yes
   ```

5. **TypeScript バインディングの生成**

   ```sh
   spacetime generate --lang typescript --module-path packages/server --out-dir packages/client/src/module_bindings
   ```

6. **クライアントの起動**

   ```sh
   pnpm dev
   ```

   [http://localhost:5173](http://localhost:5173) を **ブラウザウィンドウ2つ** で開きます。
   ただし identity は `localStorage` に永続化されるため、同じブラウザの2つのウィンドウは
   **同じプレイヤー**として再開してしまいます。2人目を別キャラとして動かすには、2つ目の
   ウィンドウを [http://localhost:5173/?guest=1](http://localhost:5173/?guest=1) で開きます
   （`?guest=1` はトークンの保存・読み込みを一切行わず、ロードのたびに使い捨ての identity に
   なります）。最初の起動では名前入力オーバーレイが表示されるので、名前を入れて開始します
   （ゲスト側でも名前は付けられますが、そのセッション限りです）。

7. **操作方法と確認ポイント**

   - 操作方法:
     - `←` `→` で移動。
     - `Space` でジャンプ。
     - `↑` でロープにつかまる／登る。
     - `↓` で降りる（足場の縁ではロープにつかまる）。
     - `↓` + `Space` でワンウェイ足場をすり抜けて降りる。
     - ロープ上で `←` / `→` + `Space` で飛び降りる。
     - `Z` 攻撃（タッチでは画面右下の `ATK` ボタン）。
     - `Enter` でチャット入力、`Esc` でゲーム操作に戻る。
   - 確認ポイント:
     - 一方のウィンドウ（`?guest=1` 側）で動かしたキャラが、もう一方の画面で**補間されて
       滑らかに動く**こと。
     - ウィンドウを閉じると、**相手の画面からそのキャラが消える**こと（行は削除されず
       `online=false` になるだけで、サーバー上には残っています）。
     - リロードしても**同じ位置・同じ名前で再開**すること（identity がトークンとして永続化
       されているため）。`?guest=1` 側はリロードのたびに新しいキャラになること。
     - 名前入力で付けた名前が**相手の画面のラベルにも反映される**こと。
     - 下から足場をすり抜けて**ジャンプで乗れる**こと（上からは着地し、下からはすり抜ける）。
     - `↓` + `Space` で足場を**すり抜けて降りられる**こと。
     - ロープで**高台（ジャンプでは届かない高さ）に登れる**こと。
     - **モブが巡回している**こと（地上のスライム・足場のキノコ）。
     - `Z` 攻撃で**モブを倒せて経験値が入る**こと（HUD の XP バーとレベルが伸びる）。
     - **倒したモブが約5秒で再出現する**こと。
     - モブに**触れるとダメージを受け**、HP が 0 になると**最初のマップ（草原）のスポーン地点に
       戻る**こと。
     - **ポータルで別マップへ移動できる**こと（足元のポータルで `↑`）。別マップに移ると**地形が
       切り替わり**、画面中央にマップ名が一瞬表示されること。
     - **別マップのプレイヤー・モブ・吹き出しが見えない**こと（同じマップにいる相手だけが見える）。
     - **チャットログは全マップ共通**で、別マップの相手の発言もログには届くこと（吹き出しは出ない）。
     - 森の **golem が強い**こと（HP が高く、接触ダメージが大きい）。
     - 片方のウィンドウで送ったチャットが、**もう片方のログと送信者の頭上の吹き出し**の
       両方に出ること。
     - キャラに**歩行・ジャンプ・登り・攻撃のモーション**が付いていること（移動速度に
       合わせて足が振れる／ジャンプ中は脚を畳む／ロープでは手を交互に伸ばす／`Z` で腕を振る）。
     - **スライムが伸縮（squash-and-stretch）して跳ね**、**キノコが歩くとよたよた揺れる**こと。
     - スクロールすると**遠景の丘や星・雲が視差（パララックス）でゆっくり流れる**こと。

8. **型チェックとテスト**

   ```sh
   pnpm typecheck   # 全パッケージの tsc --noEmit
   pnpm test        # shared の物理シミュレーションのユニットテスト
   ```

## デプロイ（公開手順）

ローカルでの動作確認ができたら、Maincloud（SpacetimeDB のマネージドホスト）へモジュールを公開し、
クライアントを Vercel へデプロイすることで、スマホを含む任意の端末からアクセスできます。

1. **Maincloud へのモジュール公開**（お手元で実行）

   ```sh
   spacetime login
   spacetime publish <DB名> --server maincloud --yes   # リポジトリルートで実行。DB名は任意のユニーク名
   ```

   TypeScript バインディングは生成済みのものがリポジトリに含まれているため、デプロイ時に再生成する必要はありません。

   > **注意**: これまでの変更で `player` テーブルに列が追加され（HP・XP・レベル等）、`mob` /
   > `mob_ai_timer` テーブルが新設され、`message` テーブル（ワールドチャット）も新設されました。
   > さらに今回 `player` テーブルに **`map_id` 列**（現在のマップ）が追加されています。
   > スキーマが変わっているため既存のモジュールがある場合は**再 publish が必須**です。
   > publish が既存データとの非互換を理由に失敗するときは、まだ永続的な進行データはないので、
   > **`--delete-data` でデータを削除して publish し直してください**
   > （例: `spacetime publish <DB名> --server maincloud --delete-data --yes`）。`--delete-data` を
   > 付けると `init` が再実行され、モブが再シードされます。ローカル（`--server local`）でも同様です。

2. **Vercel プロジェクトの作成**

   [vercel.com](https://vercel.com) で **Add New → Project** からこのリポジトリを import し、次の2点だけ設定します。

   - **Root Directory**: `packages/client`（`vercel.json` が install / build / 出力先を定義済み。Framework は Vite として自動検出されます）
   - **Environment Variables**: `VITE_SPACETIME_DB` = Maincloud で付けた DB名。
     （`VITE_SPACETIME_URI` は本番ビルドの既定が `wss://maincloud.spacetimedb.com` なので、Maincloud を使う限り設定不要です）

3. **デプロイ**

   **Deploy** を押せば公開されます。以後、ブランチへ push するたびに Vercel が自動でビルド・デプロイします。
   発行された URL にスマホからアクセスできます（タッチ操作に対応しています）。

4. **CI**

   push のたびに **CI** ワークフローが走り、typecheck・test・build・fallow（dead-code / dupes）を自動で検証します。

## やらないこと（このフェーズの範囲外）

- **見た目**は**プロシージャル描画（外部アセットなし）**でキャラ・モブ・背景をアニメーション
  しています（PixiJS の Graphics/Container のみで描画。画像アセットやフォントの読み込み、
  ネットワーク取得は一切ありません）。歩行・ジャンプ・登り・攻撃のモーション、スライムの伸縮、
  視差背景までを含みます。
- **ラグ補償（lag compensation）や巻き戻し当たり判定** は未実装です。本フェーズはクライアント予測と
  reconciliation までを対象とします。
